// const getNameHandler = (req,res)=>{
//     const {name} = req.query;
//     if(name) res.status(200).send (`Estos son los dirvers encontrados por ${name}`);
//     res.status(200).send(`Todos los corredores`);
// };

// module.exports = {getNameHandler};
